// Setup de vitest. Deliberadamente vacío por ahora: los tests de la Fase 1 son de matemática de color y
// de módulos puros, y no necesitan DOM ni mocks globales. Existe porque `vitest.config.ts` lo referencia
// y porque las fases 3+ van a necesitar `@testing-library/react` con cleanup automático.
export {};
